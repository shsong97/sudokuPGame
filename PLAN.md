# sudokuPGame 개발 플랜

> 상태: **확정안 v1** · 최종 수정: 2026-09-23 · 진행: **5단계 완료** → Play Console 내부 테스트 업로드 대기 ([docs/RELEASE.md](docs/RELEASE.md))
> `⚠️ 확인 필요` 표시 항목은 기본값으로 진행하며, 다르게 원하시면 알려주세요.

---

## 0. 확정된 결정 사항

| 항목 | 결정 |
|---|---|
| 앱 이름 | **sudokuPGame** |
| 플랫폼 | Kotlin 네이티브 (Android 전용) |
| 퍼즐 공급 | 미리 생성한 퍼즐을 앱에 내장 (앱에서 실시간 생성하지 않음). 1.0.0은 난이도별 25개(100개) → **난이도별 100개(400개)**로 확장 |
| 실수 규칙 | 실수 **3번이면 게임 오버** |
| 수익화 | 무료 (1차에는 광고·결제 없음) |
| 동물 모드 | 1~9를 동물 이모지로 표시하는 모드 추가 (새 게임·퍼즐 목록에서 선택, 기록은 숫자 모드와 공유) |
| 광고 자리 | 모든 화면 하단(내비게이션 바 위)에 배너 높이 60dp 확보, 본문은 그만큼 위로. 광고 SDK는 아직 미적용 (디버그 빌드에만 "광고 영역" 표시) |

---

## 1. 목표와 범위

**목표**: 오프라인에서 가볍게 즐길 수 있는 무료 스도쿠 앱.

**MVP 범위 (1차 출시)**
- 9×9 클래식 스도쿠
- 난이도 4단계: 쉬움 / 보통 / 어려움 / 전문가
- 내장 퍼즐 400개, 난이도별 100개 (모두 유일해 검증 완료)
- 메모(후보 숫자) 입력, 실행 취소, 지우개, 힌트
- 실수 3회 제한 → 게임 오버
- 타이머, 진행 중인 게임 자동 저장 / 이어하기
- 퍼즐별 클리어 여부 표시, 난이도별 통계(최고 기록, 평균 시간, 승률)
- 라이트 / 다크 테마

**MVP 이후 (2차 이후)**
- 퍼즐 팩 추가 (앱 업데이트로 배포)
- 데일리 챌린지, 업적
- 클라우드 백업 (Google Play Games)
- 다국어 (한국어 → 영어 순)

**범위 외**: iOS, 멀티플레이, 광고/결제

---

## 2. 기술 스택

| 영역 | 선택 |
|---|---|
| 언어 | Kotlin |
| UI | Jetpack Compose + Material 3 |
| 아키텍처 | MVVM + 단방향 데이터 흐름 (ViewModel + StateFlow) |
| 의존성 주입 | Hilt |
| 로컬 저장 | Room (진행 상태·기록) + DataStore (설정) |
| 직렬화 | kotlinx.serialization (내장 퍼즐 JSON 로드) |
| 비동기 | Kotlin Coroutines |
| 내비게이션 | Navigation Compose |
| 테스트 | JUnit5, Turbine, Compose UI Test |
| 최소 SDK | API 26 (Android 8.0) |
| 컴파일 / 타깃 SDK | 37 / 36 |
| 빌드 | Gradle 9.7.1 (Kotlin DSL) + Version Catalog, AGP 9.4.1, Kotlin 2.4.20 |
| 아이콘 | Material 아이콘 라이브러리 대신 벡터 드로어블 리소스 사용 |

- **패키지명**: `com.sudokupgame.app` ⚠️ 확인 필요 — 스토어 출시 후에는 바꿀 수 없으니, 본인 소유 도메인이 있으면 그걸 기준으로 정하는 게 좋습니다.

---

## 3. 핵심 설계

### 3.1 모듈 구조

```
sudokuPGame/
 ├─ engine/            # 순수 Kotlin(JVM) 모듈: 보드 모델, 솔버, 생성기, 난이도 평가
 ├─ puzzle-generator/  # 개발용 CLI: engine으로 퍼즐 생성 → JSON 출력 (앱에 포함 안 됨)
 └─ app/               # 안드로이드 앱
     └─ com.sudokupgame.app
         ├─ data/      # 퍼즐 로더(assets), Room, DataStore, Repository
         ├─ ui/        # 테마, 공통 컴포넌트
         └─ feature/
             ├─ home/      # 메인, 이어하기
             ├─ puzzles/   # 퍼즐 목록(난이도별)
             ├─ game/      # 게임 화면
             ├─ stats/     # 통계
             └─ settings/  # 설정
```

`engine`은 앱(힌트·검증)과 `puzzle-generator`(생성) 양쪽에서 사용합니다.

### 3.2 내장 퍼즐 파이프라인

```
[개발 시] puzzle-generator 실행
   → 완성 보드 생성 (무작위 백트래킹)
   → 칸을 하나씩 비우며 매번 유일해 검사
   → 풀이 기법 기반 난이도 평가 → 목표 난이도에 맞는 것만 채택
   → app/src/main/assets/puzzles.json 저장 (Git에 커밋)
[런타임] 앱이 puzzles.json 로드 → 게임 제공
```

**난이도 배분**: 쉬움 100 / 보통 100 / 어려움 100 / 전문가 100

**퍼즐 추가 규칙**: 기존 퍼즐의 id·내용·순서는 절대 바꾸지 않는다 (기기의 기록·진행 중 게임이 id로 가리킴).
생성기는 기존 파일을 읽어 모자란 만큼만 뒤에 추가하고, 추가분끼리만 쉬운 순으로 정렬한다.
처음 출시한 100개(E001~X025)는 지문(SHA-256) 테스트로 고정되어 있다.
혹시 퍼즐 데이터가 바뀌어 저장된 게임과 주어진 숫자가 다르면, 이어하기 대신 새로 시작한다.

| 난이도 | 필요한 기법 | 대략적 힌트 수 |
|---|---|---|
| 쉬움 | Naked Single, Hidden Single | 36~45 |
| 보통 | + Naked/Hidden Pair, Pointing | 30~35 |
| 어려움 | + Triple, Box/Line Reduction, X-Wing | 26~30 |
| 전문가 | + Swordfish, XY-Wing 등 | 22~26 |

**퍼즐 JSON 형식**
```json
{
  "version": 1,
  "puzzles": [
    {
      "id": "E001",
      "difficulty": "EASY",
      "givens":   "530070000600195000098000060800060003400803001700020006060000280000419005000080079",
      "solution": "534678912672195348198342567859761423426853791713924856961537284287419635345286179"
    }
  ]
}
```
81자 문자열(`0` = 빈칸). 400개 전체 용량은 약 104KB.

**생성 방법**: `./gradlew :puzzle-generator:run --args="--count=100"` — 기존 파일을 유지하고 난이도별 100개까지 채운다. 같은 시드(기본 2026)·같은 기존 파일이면 항상 같은 결과 (400개 약 6초). 대칭 모양을 위해 180° 회전 대칭 쌍으로 칸을 비웁니다.

**생성 시 검증 (CI에서도 실행)**
- 모든 퍼즐의 해가 정확히 1개인지
- `solution`이 스도쿠 규칙을 만족하고 `givens`와 일치하는지
- 난이도 평가 결과가 표기된 난이도와 같은지
- 중복 퍼즐(회전·숫자 치환 포함)이 없는지

**모두 풀면?** 1차에서는 클리어한 퍼즐을 다시 플레이할 수 있게 합니다. ⚠️ 확인 필요 — 대안으로, 같은 퍼즐에 숫자 치환·행/열 교환·회전을 적용해 "모양이 다른" 퍼즐로 바꿔 제공할 수도 있습니다 (난이도와 유일해는 그대로 유지됨).

### 3.3 도메인 모델 (개요)

```kotlin
data class Cell(
    val value: Int?,          // 1~9 또는 빈칸
    val isGiven: Boolean,     // 처음부터 주어진 숫자 (수정 불가)
    val notes: Set<Int>,      // 메모
    val isError: Boolean,     // 정답과 다른 숫자
)

data class GameState(
    val puzzleId: String,
    val cells: List<Cell>,    // 81칸, index = row * 9 + col
    val solution: IntArray,
    val difficulty: Difficulty,
    val elapsedSeconds: Long,
    val mistakes: Int,        // 0~3
    val hintsUsed: Int,
    val history: List<Move>,  // 실행 취소용
    val status: GameStatus,   // PLAYING, PAUSED, WON, LOST
)
```

### 3.4 실수 / 게임 오버 규칙
- **실수** = 빈칸에 정답과 다른 숫자를 입력한 경우 (메모 입력은 실수 아님)
- 틀린 숫자는 즉시 빨간색으로 표시되고 실수 카운트 +1 (상단에 `실수 1/3` 표시)
- 실행 취소를 해도 실수 카운트는 줄어들지 않음
- 정답으로 채운 칸은 확정(잠금)되어 지우거나 바꿀 수 없음. 오답 칸은 지우개로 지우거나 다른 숫자로 덮어쓸 수 있음
- 3번째 실수 → 게임 오버 다이얼로그:
  - **다시 시작**: 같은 퍼즐을 처음부터
  - **다른 퍼즐**: 퍼즐 목록으로
- 게임 오버는 통계에 "패배"로 기록

### 3.5 힌트 시스템
- 엔진의 풀이 기법을 재사용해 **다음에 적용할 수 있는 기법과 해당 칸**을 보여주는 설명형 힌트
- 2단계: ① 칸과 기법 이름 강조 → ② 정답 공개
- 다음으로 **숫자를 확정할 수 있는 칸**까지 풀이를 진행하고, 그 과정에서 쓴 가장 어려운 기법을 설명 (후보만 지우는 기법은 메모 없이 설명하기 어렵기 때문)
- 틀린 숫자가 있으면 힌트가 먼저 그 칸을 알려주고, ② 단계에서 지워줌
- 힌트 사용 횟수는 결과 화면과 통계에 기록 (힌트 요청 1번 = 1회, 횟수 제한 없음)

### 3.6 동물 모드

- 1 🐶 개 · 2 🐱 고양이 · 3 🦁 사자 · 4 🐵 원숭이 · 5 🐰 토끼 · 6 🐻 곰 · 7 🐼 판다 · 8 🐘 코끼리 · 9 🐧 펭귄
- 같은 100개 퍼즐을 표시만 바꿔서 제공. 규칙·힌트·기록·통계는 숫자 모드와 같음 (한 퍼즐은 어느 모드로든 한 번 클리어하면 됨)
- 새 게임 바텀시트와 퍼즐 목록 상단에서 모드 선택, 마지막 선택을 기억
- 진행 중인 게임에 모드를 저장 (DB v3) → 이어하기는 같은 모드로. 목록에서 다른 모드로 고르면 진행 상황은 유지하고 표시만 바뀜
- 이모지는 색을 바꿀 수 없으므로: 오답은 빨간 테두리, 직접 넣은 칸은 옅은 배경, 다 쓴 동물은 숫자 패드에서 흐리게
- 힌트 문구와 화면 낭독(TalkBack)도 "숫자" 대신 동물 이름 사용

### 3.7 저장
- 게임 상태는 입력마다 debounce(약 500ms)해서 Room에 저장, 앱이 백그라운드로 가거나 게임 화면을 떠나면 즉시 저장
- 경과 시간은 10초 단위로도 저장 (강제 종료 시 잃는 시간 최대 10초)
- 저장은 앱 전체 코루틴 스코프에서 순서대로 실행 (화면을 떠나도 끝까지 완료)
- 실행 취소 기록은 저장하지 않음 (이어하기 후에는 실행 취소 불가)
- 진행 중인 게임은 1개만 유지 (새 퍼즐을 시작하면 확인 후 덮어씀)
- 퍼즐별 기록 테이블: `puzzleId`, 클리어 여부, 최고 기록, 시도 횟수

---

## 4. 화면 구성

1. **홈**: 이어하기(진행 중 게임이 있을 때), 새 게임, 퍼즐 목록, 통계, 설정
   - "새 게임" = 선택한 난이도에서 아직 안 푼 퍼즐 중 다음 번호
2. **퍼즐 목록**: 난이도 탭 → 번호 격자 (클리어한 퍼즐은 체크 + 최고 기록)
3. **게임**
   - 상단: 퍼즐 번호·난이도, `실수 n/3`, 타이머(일시정지 시 격자 가림)
   - 중앙: 9×9 격자 — 선택한 칸의 행·열·박스 및 같은 숫자 강조
   - 하단: 도구(실행 취소 / 지우개 / 메모 / 힌트), 숫자 패드 1~9 (다 채운 숫자는 비활성)
   - 클리어 → 결과 다이얼로그 (시간, 실수, 힌트, 최고 기록 여부) / 3번 실수 → 게임 오버 다이얼로그
4. **통계**: 난이도별 — 클리어 수(n/100), 승률, 최고/평균 시간
5. **설정**: 같은 숫자 강조, 자동 메모 정리, 타이머 표시, 테마, 진동

---

## 5. 개발 일정 (1인 개발 기준, 약 7주)

| 단계 | 기간 | 주요 작업 | 완료 기준 |
|---|---|---|---|
| 0. 셋업 ✅ | 0.5주 | 멀티모듈 프로젝트 생성, Gradle, Hilt, 테마, CI(GitHub Actions) | 빈 앱 빌드·실행 |
| 1. 엔진 + 퍼즐 생성 ✅ | 1.5주 | 보드 모델, 솔버, 기법 기반 난이도 평가, 생성기 CLI, 검증 테스트 | `puzzles.json` 100개 생성 및 검증 통과 |
| 2. 게임 화면 ✅ | 2주 | 격자 렌더링, 입력, 메모, 실행 취소, 실수 3회 규칙, 타이머, 클리어/게임 오버 | 퍼즐 하나를 처음부터 끝까지 플레이 가능 |
| 3. 저장·홈·목록 ✅ | 1주 | Room/DataStore, 이어하기, 홈, 퍼즐 목록, 설정 | 앱 재실행 후 이어하기, 클리어 표시 유지 |
| 4. 통계·힌트 ✅ | 1주 | 통계 화면, 설명형 힌트 | |
| 5. 마무리 ✅ | 1주 | 다크모드·접근성·애니메이션, 앱 아이콘, 스토어 등록 자료 | Play Console 내부 테스트 트랙 배포 |

---

## 6. 품질 / 테스트 전략
- **엔진**: 알려진 퍼즐 풀이, 유일해 검사, 난이도 평가 단위 테스트 (커버리지 90% 이상 목표)
- **퍼즐 데이터**: `puzzles.json` 전체 검증 테스트를 CI에서 매번 실행
- **ViewModel**: 입력 → 상태 변화, 실수 3회 → LOST 전환 테스트 (Turbine)
- **UI**: 주요 흐름(새 게임 → 입력 → 클리어 / 게임 오버) Compose UI 테스트
- **수동 점검**: 소형 폰·태블릿, 가로 모드, 글꼴 크기 확대, TalkBack

---

## 7. 리스크와 대응

| 리스크 | 대응 |
|---|---|
| 전문가 난이도 퍼즐 생성이 오래 걸림 | 개발 시 한 번만 생성하므로 수 분~수십 분이 걸려도 문제없음 |
| 퍼즐을 금방 소진 | 재플레이 허용, 이후 퍼즐 팩 업데이트 또는 변형 적용 (3.2 참고) |
| 난이도 체감이 들쭉날쭉 | 기법별 가중치 점수로 같은 난이도 안에서도 쉬운 순으로 정렬 |
| 실수 3회 규칙이 초보자에게 가혹함 | 1차는 규칙대로 출시, 반응을 보고 쉬움 난이도 완화 검토 |
| 작은 화면에서 터치가 어려움 | 셀 최소 크기 확보, 칸 선택 후 숫자 패드로 입력 |

---

## 8. 남은 확인 사항 (기본값으로 진행 예정)

1. **패키지명**: `com.sudokupgame.app`로 할까요?
2. **난이도 배분**: 25/25/25/25로 할까요?
3. **100개 소진 후**: 재플레이만 허용할까요, 아니면 변형 퍼즐도 제공할까요?
